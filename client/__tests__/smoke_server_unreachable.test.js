import { describe, it, expect } from "vitest";

// Smoke test skeleton: simulate server unreachable and verify UI recovers

describe("smoke: server unreachable", () => {
  it("shows retry/cancel when server is unreachable", () => {
    // Manual or mocked test: start app with backend disabled or mock fetch to fail
    expect(true).toBe(true);
  });
});
